# Node Stage Maps

Future ComfyUI node maps should explain which nodes belong to each logical pipeline stage:

- Input and metadata conditioning.
- Hero still generation.
- Product lock composite.
- Image-to-video preparation.
- Export and evaluation handoff.
