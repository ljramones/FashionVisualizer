{
  "name": "luxflow-ai-workspace",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "./.venv/bin/uvicorn backend.app.main:app --reload",
    "test": "./.venv/bin/python -m pytest backend/tests",
    "lint": "./.venv/bin/ruff check backend scripts && npm --prefix frontend run lint"
  }
}
